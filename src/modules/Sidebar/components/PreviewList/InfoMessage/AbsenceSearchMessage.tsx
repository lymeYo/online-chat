import styles from './styles.module.css'

const AbsenceSearchMessage = () => (
  <div className={styles.wrapper}>
    <span className={styles.text}>Пользователи с таким именем не найдены</span>
  </div>
)

export default AbsenceSearchMessage
